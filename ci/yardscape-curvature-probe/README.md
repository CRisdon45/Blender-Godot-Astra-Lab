# Curvature/orientation comparison in the same courtyard

This reviewed renderer/test-only slice retains the existing public execution boundary. Application development remains in private Yard-Scape. No private model/persistence, customer/reference files, secrets or history are added.

The new generator inherits the existing branch and group assembly and overrides foliage meshing only. Four sprays on each of 16 unchanged bough groups remain 64 sprays. Rounded irregular contours, bowed surfaces and varied orientations replace shallow star fans. No shader, palette, light, site geometry or branch mesh changes. This spends more triangles per group to test curvature, not a claim of faster rendering.

A small factory seam in the existing wrapper defaults to the unchanged folded generator. The new scene alone selects the curved subtype. The quick test renders matched folded/curved Plan, courtyard, eye-level and reverse-eye-level views before further testing. Full mode reuses the existing canopy probe, changing only its three scene/class selection paths in a staged copy. It preserves all original assertions and thresholds, including alternate-seed tests against the new generator.

The existing two spatial read-only adapters remain explicitly unmodified. Neither quick nor full mode tests private editing/undo/save or physical-tablet performance. Native execution stays on the authorized Linux GitHub worker with the exact pinned engine. Evidence artifacts include only PNGs, reports, logs and hashes. All scene components remain real 3D; no screenshot plane, alpha foliage, runtime AI or private data is introduced. No new blanket license is assigned to original owner code.
